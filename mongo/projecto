21-11-2022
https://www.youtube.com/watch?v=YlrmGs6YHHY&t=311s&ab_channel=EduonixLearningSolutions

document database (mongodb,couchdb,rethinkdb)
key/value store(dynamo,riak)
column database(cassandra,kdb,sybase)
graph database (neo4j)
caching system (redis,memcached)

mongodb overview
open source document database
cross platform (windows,linux,mac)
created in 2007 by 10gen [mongodb]
used by craiglist ebay foursquare and much more
se usa en:
big data
social networks
cloud computing

structurado
sin estructura
semi-estructurado 

min 6

supports tb of data
new development methods & technology

document data model
data stored in documents
bson / json syntax
values can contain strings,numbers,arrays,objects
ObjectId,Null
documents stored in collections

{
   _id:1
   name: "Jhon Doe",
   email : "jdoe@gmail.com",
   age : 34,
   address:{
         street:"5 school st",
         city : "New York",
         state : "NY",                  
   },
   memberships:["standard","super"]
}

install mongodb in ubuntu
import the public key
sudo apt-key adv ......
terminal ubuntu
insert code
insert password
sudo apt get install


min 10

sudo service mongod start
mongo
show dbs
admin 0gb
local 0gb

installing mongodb in mac osx
install homebrew
copy link
call brew
brew update

install a mongodb
brew install mongodb
create a data directory
sudo mkdir 
passoword
sudo chown
mongod
mongo

windows
descargar
tiene una ruta de instalacion por defecto

cd. = directorio actual
cd.. = retroceder
C:\Users>cd ..
C:\>cd Program Files

mongodb
cd mongodb = no importa si escribe en minuscula lo encuentra igual

min 15







   
   
   
   











